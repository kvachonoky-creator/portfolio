import styled from "styled-components";

export const Container = styled.div`
    max-width: 1280px;
    width: 100%;
    min-height: 100%;
    padding: 0 clamp(20px, 4vw, 56px);
    margin: 0 auto;
`;
